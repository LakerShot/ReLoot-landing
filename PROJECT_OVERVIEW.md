# ReLoot Landing - Обзор Проекта

## 📋 Описание

**ReLoot Landing** - это современное веб-приложение для продающего лендинга, построенное на базе монорепозитория с использованием **Next.js** и **Tamagui**. Проект оптимизирован для SEO и создания высокопроизводительных посадочных страниц.

## 🏗️ Архитектура Монорепозитория

Проект использует **монорепозиторий** с разделением на приложения и переиспользуемые пакеты:

```
ReLoot-landing/
├── apps/                    # Приложения
│   └── next/              # Web приложение (Next.js)
│       ├── app/           # App Router (Next.js 13+)
│       ├── pages/         # Pages Router (legacy)
│       ├── public/        # Статические файлы
│       └── __tests__/     # Тесты
│
├── packages/              # Переиспользуемые пакеты
│   ├── app/              # Бизнес-логика приложения
│   │   ├── features/     # Функциональность по фичам
│   │   │   ├── home/    # Домашняя страница
│   │   │   └── user/    # Пример страницы пользователя
│   │   └── provider/    # React провайдеры
│   ├── ui/               # UI Kit (@my/ui)
│   │   └── src/         # Tamagui компоненты
│   └── config/           # Конфигурация (@my/config)
│       └── src/         # Настройки Tamagui, темы, токены
│
├── .github/             # GitHub Actions
├── .husky/              # Git hooks
└── node_modules/        # Зависимости
```

## 🛠️ Технологический Стек

### Основные Технологии

| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **Node.js** | 22.x | Runtime окружение |
| **Yarn** | 4.5.0 | Менеджер пакетов |
| **TypeScript** | ~5.8.3 | Типизация |
| **React** | 19.0.0 | UI библиотека |
| **React Native** | 0.79.2 | Native разработка |

### Фреймворки и Инструменты

#### Web Platform
- **Next.js** `14.2.14` - React фреймворк с App Router для SSR/SSG
- **React DOM** `19.0.0` - Рендеринг для браузера
- **React Native Web** `^0.20.0` - Адаптер для Tamagui компонентов на вебе

#### UI & Styling
- **Tamagui** `^1.138.6` - Высокопроизводительная UI система с compile-time оптимизацией
  - `@tamagui/lucide-icons` - Иконки
  - `@tamagui/themes` - Система тем
  - `@tamagui/toast` - Уведомления
  - `@tamagui/next-theme` - Интеграция с Next.js
- **React Native SVG** `15.15.1` - Поддержка SVG для иконок

#### Development Tools
- **Turbo** `^1.13.4` - Система сборки монорепозитория
- **Vitest** `^2.1.1` - Тестирование
- **Biome** `^1.9.3` - Линтер и форматер
- **Prettier** `^3.3.3` - Форматирование кода
- **Husky** `^9.1.6` - Git hooks

## 🚀 Команды Запуска

### Установка Зависимостей

```bash
# Установка всех зависимостей монорепозитория
yarn install

# или
yarn
```

### Разработка

```bash
# Web разработка (Next.js)
yarn web                    # Запуск dev-сервера (localhost:3000)
yarn web:extract           # Запуск с Tamagui extraction (для тестирования оптимизации)
yarn web:prod              # Production сборка
yarn web:prod:serve        # Запуск production сборки (порт 8151)
```

### Сборка

```bash
# Сборка всех пакетов (кроме next-app)
yarn build

# Сборка только web приложения
yarn web:prod
```

### Тестирование

```bash
# Запуск тестов (Vitest)
yarn test                  # Разовый запуск
yarn test:watch            # Watch режим
```

### Утилиты

```bash
# Обновление Tamagui
yarn upgrade:tamagui        # Stable версия
yarn upgrade:tamagui:canary # Canary версия

# Проверка Tamagui конфигурации
yarn check-tamagui

# Watch режим для разработки пакетов
yarn watch
```

## 📦 Управление Зависимостями

### Общие Зависимости

Для зависимостей, используемых в бизнес-логике:

```bash
cd packages/app
yarn add [package-name]
cd ../..
yarn
```

**Примеры:** `date-fns`, `lodash`, `zod`

### UI Компоненты

Для новых UI компонентов:

```bash
cd packages/ui
yarn add [package-name]
cd ../..
yarn
```

**Примеры:** `@tamagui/lucide-icons`, дополнительные Tamagui пакеты

### Next.js Зависимости

Для зависимостей, специфичных для Next.js:

```bash
cd apps/next
yarn add [package-name]
cd ../..
yarn
```

**Примеры:** `next-seo`, `@vercel/analytics`

## 🎨 UI Kit (@my/ui)

Проект использует собственную систему дизайна на базе Tamagui:

```typescript
// packages/ui/src/index.tsx
export * from './MyComponent'
export * from './Button'
// ... другие компоненты
```

### Создание Нового Компонента

1. Создайте файл в `packages/ui/src/YourComponent.tsx`
2. Экспортируйте компонент из `packages/ui/src/index.tsx`
3. Используйте в приложениях:

```typescript
import { YourComponent } from '@my/ui'
```

## 🧪 Тестирование

Проект использует **Vitest** для тестирования:

```typescript
// apps/next/__tests__/example.test.ts
import { describe, it, expect } from 'vitest'

describe('Example Test', () => {
  it('should work', () => {
    expect(true).toBe(true)
  })
})
```

## 🌐 Деплой

### Vercel (Рекомендуется)

**Настройки проекта:**
- **Framework Preset:** Next.js
- **Root Directory:** `apps/next`
- **Install Command:** `yarn set version stable && yarn install`
- **Build Command:** `yarn build` (из корня) или оставить по умолчанию
- **Output Directory:** `.next` (по умолчанию)

### Другие платформы

Проект совместим с любыми платформами, поддерживающими Next.js:
- Netlify
- AWS Amplify
- Cloudflare Pages
- Self-hosted с Node.js

## 🔧 Конфигурация

### TypeScript

- `tsconfig.base.json` - Базовая конфигурация
- `tsconfig.json` - Корневая конфигурация
- Каждый пакет/приложение имеет свой `tsconfig.json`, наследующий базовую конфигурацию

### Turbo

`turbo.json` - конфигурация системы сборки:
- Управление кешированием
- Определение порядка сборки пакетов
- Параллельное выполнение задач

### Biome

`biome.json` - настройки линтера и форматера:
- Правила линтинга
- Стиль кодирования
- Игнорируемые файлы

## 🎯 Best Practices

### Организация Кода

1. **Features First** - организуйте код по фичам, а не по типам файлов:
   ```
   packages/app/features/
   ├── home/
   │   ├── screen.tsx
   │   ├── hooks.ts
   │   └── types.ts
   └── user/
       └── screen.tsx
   ```

2. **Shared Components** - переиспользуемые компоненты в `packages/ui`

3. **Business Logic** - бизнес-логика в `packages/app`

### Навигация

Используйте стандартные компоненты Next.js:

```typescript
import Link from 'next/link'
import { Anchor } from '@my/ui'

// Next.js Link
<Link href="/user/123">
  <a>Go to User</a>
</Link>

// Или Tamagui Anchor
<Anchor href="/user/123" target="_self">
  Go to User
</Anchor>
```

### Стилизация

Используйте **Tamagui** для кросс-платформенных стилей:

```typescript
import { YStack, Text } from 'tamagui'

export function MyComponent() {
  return (
    <YStack padding="$4" backgroundColor="$background">
      <Text fontSize="$6" color="$color">
        Hello World
      </Text>
    </YStack>
  )
}
```

## 📱 Режимы Роутинга Next.js

Проект использует **App Router** (Next.js 13+):
- Основные страницы: `apps/next/app/`
- Legacy примеры: `apps/next/pages/` (можно удалить)

### Преимущества App Router
- React Server Components
- Улучшенная производительность
- Встроенная поддержка layouts
- Оптимизация для SEO

## 🐛 Отладка

### Next.js Compiler

Добавьте комментарий `// debug` в начало файла для вывода отладочной информации компилятора Tamagui.

### Логирование

```typescript
console.log('[Feature]:', data) // Используйте префиксы для удобства
```

## 🔗 Полезные Ссылки

- [Tamagui Documentation](https://tamagui.dev)
- [Next.js Documentation](https://nextjs.org/docs)
- [React 19 Documentation](https://react.dev)
- [Vercel Platform](https://vercel.com)
- [TypeScript Documentation](https://www.typescriptlang.org/docs)

## 🤝 Contributing

1. Создайте feature ветку
2. Внесите изменения
3. Убедитесь, что тесты проходят: `yarn test`
4. Проверьте форматирование: `yarn biome check .`
5. Создайте Pull Request

## 📄 Лицензия

Private project

---

**Создано:** 2025-12-04  
**Обновлено:** 2025-12-04  
**Node.js:** v22.14.0  
**Yarn:** 4.5.0  
**Статус:** ✅ Очищено от Expo/RN, работает Next.js + Tamagui
